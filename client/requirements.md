## Packages
framer-motion | For smooth page transitions, list staggering, and polished micro-interactions

## Notes
The backend endpoints and schemas dictate that the find match feature uses a POST request to /api/match which returns a standard array of MatchResult objects.
Coordinates for the UI are prefilled with a default (e.g., 40.7128, -74.0060) to ease testing.
